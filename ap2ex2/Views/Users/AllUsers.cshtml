@model List<User>

<div class="formBox">
<a asp-action="Login" class="btn btn-primary">Logout</a>
    <table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>NickName</th>
        <th>Password</th>
        <th>Details</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Name</td>
                <td>@user.Password</td>
                <td><a asp-action="Details" asp-route-id="@user.Id" class="btn btn-primary">Details</a></td>
            </tr>
        }
    </tbody>
</table>
</div>